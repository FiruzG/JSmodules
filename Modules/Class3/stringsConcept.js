/**
 * To compare if two Strings are equal
 * function: localCompare
 * 
 * if both String are equal, function return 0
 * 
 */

let word = 'laUgH';

let toLowerCase = word.toLowerCase();
let toUperCase = toLowerCase.replace(toLowerCase.toUpperCase(toLowerCase.charAt(0)));
console.log(toUperCase);