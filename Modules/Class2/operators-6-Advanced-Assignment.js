/**
 * Advanced Assignment (+=, -=, *=, /=, %=)
 */

let num1 = 5;

num1 += 5; // same as num1 = num1 +5;


/**
 * Banking
 * currentBalance = 1000
 * 
 * deposit = 200
 * curentBalance = curentBalance + deposit is = to curentBalance += deposit
 */